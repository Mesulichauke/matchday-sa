import { FormEvent, useState } from 'react';
import { BriefcaseBusiness, Check, MapPin, Users } from 'lucide-react';
import Layout from '@/components/Layout';

const packages = [
  { id: 'team-bonding', title: 'Team Bonding Matchday', price: 1599, minGuests: 10, icon: Users, description: 'An energetic staff day with transport, team challenges, local hosts and a shared shisanyama lunch.', features: ['Facilitated team challenge', 'Township walking route', 'Lunch and local host', 'Office pickup planning'] },
  { id: 'executive-lounge', title: 'Executive Matchday Lounge', price: 2499, minGuests: 8, icon: BriefcaseBusiness, description: 'A premium, host-led experience designed for leadership teams, clients and partner hospitality.', features: ['Dedicated itinerary host', 'Premium hospitality flow', 'Private group transport', 'Concierge pickup planning'] },
  { id: 'heritage-impact', title: 'Heritage & Impact Day', price: 2999, minGuests: 10, icon: MapPin, description: 'A purpose-led community experience for departments, agencies and teams looking to connect with local history.', features: ['Heritage storytelling', 'Community project visit', 'Cultural experience and lunch', 'Impact-focused itinerary'] },
];

const officePickups = ['Sandton Office · Johannesburg', 'Rosebank Office · Johannesburg', 'Pretoria HQ · Pretoria', 'Cape Town Operations · Cape Town', 'Durban Corporate Hub · Durban', 'Gqeberha Business Hub · Gqeberha'];

export default function Corporate() {
  const [selectedPackageId, setSelectedPackageId] = useState(packages[0].id);
  const [company, setCompany] = useState('');
  const [email, setEmail] = useState('');
  const [guests, setGuests] = useState(10);
  const [pickup, setPickup] = useState('');
  const [date, setDate] = useState('');
  const [notes, setNotes] = useState('');
  const selectedPackage = packages.find((item) => item.id === selectedPackageId) ?? packages[0];
  const total = selectedPackage.price * guests;

  const choosePackage = (id: string) => {
    const pkg = packages.find((item) => item.id === id) ?? packages[0];
    setSelectedPackageId(pkg.id);
    setGuests((current) => Math.max(current, pkg.minGuests));
    document.getElementById('corporate-quote')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  };

  const submitEnquiry = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const body = `Hello Matchday SA,\n\nPlease prepare a corporate package quote.\n\nCompany: ${company}\nPackage: ${selectedPackage.title}\nGuests: ${guests}\nEstimated total: R${total}\nPreferred date: ${date || 'To be discussed'}\nPickup: ${pickup}\nContact email: ${email}\nNotes: ${notes || 'None'}\n\nThank you.`;
    window.location.href = `mailto:info@matchday.sa?subject=${encodeURIComponent(`Corporate enquiry: ${selectedPackage.title}`)}&body=${encodeURIComponent(body)}`;
  };

  return <Layout><div className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><section className="mx-auto max-w-3xl text-center"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-gold-500">Corporate experiences</p><h1 className="mt-3 text-4xl font-bold sm:text-5xl">Team building with real local energy.</h1><p className="mt-4 text-lg text-gray-400">Hosted township experiences built for staff connection, leadership groups, client hospitality and purpose-led team days.</p></section><section className="mt-12 grid gap-6 lg:grid-cols-3">{packages.map((pkg) => { const Icon = pkg.icon; const active = pkg.id === selectedPackageId; return <article key={pkg.id} className={`rounded-2xl border p-6 transition-colors ${active ? 'border-gold-500 bg-gold-500/10' : 'border-white/10 bg-white/5 hover:border-gold-500/50'}`}><div className="flex items-start justify-between gap-4"><div className="flex h-12 w-12 items-center justify-center rounded-lg bg-gold-500/15 text-gold-500"><Icon className="h-6 w-6" /></div>{active && <span className="rounded-full bg-gold-500 px-3 py-1 text-xs font-semibold text-black">Selected</span>}</div><h2 className="mt-5 text-xl font-semibold">{pkg.title}</h2><p className="mt-3 text-sm leading-6 text-gray-400">{pkg.description}</p><ul className="mt-5 space-y-2">{pkg.features.map((feature) => <li key={feature} className="flex gap-2 text-sm text-gray-300"><Check className="h-4 w-4 shrink-0 text-green-400" />{feature}</li>)}</ul><div className="mt-6 flex items-end justify-between border-t border-white/10 pt-4"><div><p className="text-2xl font-bold text-gold-500">R{pkg.price}</p><p className="text-xs text-gray-400">per guest · min {pkg.minGuests}</p></div><button type="button" onClick={() => choosePackage(pkg.id)} className="rounded-md bg-gold-500 px-4 py-2 text-sm font-semibold text-black hover:bg-gold-600">Choose package</button></div></article>})}</section><section className="mt-12 grid gap-8 lg:grid-cols-[0.9fr_1.1fr]"><div className="rounded-2xl border border-white/10 bg-white/5 p-6"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-gold-500">What’s included</p><h2 className="mt-3 text-2xl font-bold">Built around your team, not a generic itinerary.</h2><div className="mt-6 space-y-4">{['Dedicated team host and route planning', 'Flexible timings and group logistics', 'Township cultural storytelling and local guides', 'Office collection or custom pickup arrangements', 'Clear group pricing before confirmation'].map((item) => <p key={item} className="flex gap-3 text-sm text-gray-300"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-gold-500/15"><Check className="h-4 w-4 text-gold-500" /></span>{item}</p>)}</div><div className="mt-8 border-t border-white/10 pt-6"><p className="text-sm font-medium text-white">Office collection points</p><div className="mt-3 flex flex-wrap gap-2">{officePickups.map((point) => <span key={point} className="rounded-full bg-black/20 px-3 py-1 text-xs text-gray-300">{point}</span>)}</div></div></div><section id="corporate-quote" className="rounded-2xl border border-gold-500/25 bg-black p-6"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-gold-500">Request a quote</p><h2 className="mt-3 text-2xl font-bold">Plan {selectedPackage.title}</h2><p className="mt-2 text-sm text-gray-400">Share the essentials and we’ll tailor the route, timing and pickup plan.</p><form onSubmit={submitEnquiry} className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm text-gray-300">Company<input required value={company} onChange={(event) => setCompany(event.target.value)} placeholder="Your company" className="mt-2 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white placeholder:text-gray-500 focus:border-gold-500 focus:outline-none" /></label><label className="text-sm text-gray-300">Work email<input required type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="team@company.com" className="mt-2 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white placeholder:text-gray-500 focus:border-gold-500 focus:outline-none" /></label><label className="text-sm text-gray-300">Package<select value={selectedPackageId} onChange={(event) => choosePackage(event.target.value)} className="mt-2 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white focus:border-gold-500 focus:outline-none">{packages.map((pkg) => <option key={pkg.id} value={pkg.id} className="bg-black">{pkg.title}</option>)}</select></label><label className="text-sm text-gray-300">Number of guests<input required type="number" min={selectedPackage.minGuests} value={guests} onChange={(event) => setGuests(Math.max(selectedPackage.minGuests, Number(event.target.value) || selectedPackage.minGuests))} className="mt-2 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white focus:border-gold-500 focus:outline-none" /></label><label className="text-sm text-gray-300">Preferred date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} className="mt-2 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white focus:border-gold-500 focus:outline-none" /></label><label className="text-sm text-gray-300">Office pickup<select required value={pickup} onChange={(event) => setPickup(event.target.value)} className="mt-2 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white focus:border-gold-500 focus:outline-none"><option value="" className="bg-black">Choose a pickup point</option>{officePickups.map((point) => <option key={point} value={point} className="bg-black">{point}</option>)}</select></label><label className="text-sm text-gray-300 sm:col-span-2">Anything else we should plan for?<textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Accessibility, dietary needs, preferred activities..." className="mt-2 min-h-24 w-full rounded-md border border-white/10 bg-white/5 px-3 py-2 text-white placeholder:text-gray-500 focus:border-gold-500 focus:outline-none" /></label><div className="flex items-center justify-between rounded-lg bg-white/5 px-4 py-3 sm:col-span-2"><span className="text-sm text-gray-300">Estimated package total</span><strong className="text-xl text-gold-500">R{total}</strong></div><button type="submit" className="rounded-md bg-gold-500 px-5 py-3 font-semibold text-black hover:bg-gold-600 sm:col-span-2">Send corporate enquiry</button></form></section></section></div></Layout>;
}
